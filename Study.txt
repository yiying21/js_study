[vscode]
window powershell에서 한글 깨질 때 선언 후 시작

[System.Console]::OutputEncoding = [System.Text.Encoding]::UTF8


전체 검색 : 컨트롤 + 시프트 + P
브라우저에서 콘솔열기 : 컨트롤 + 시프트 + i

window PowerShell 관리자 모드로 실행
파일 경로 바꿈
cd 파일경로 주소


[nodejs]
결과값 보기
node week01/main.js

부분 저장
git add .gitignore
git commit -m "입력"

현재 저장 상태
git status

전체 저장
git add .
git commit -m "입력"

저장 로그
git log

올라와 있는 정보를 가져온 후 업로드
git pull

git hub 업로드
git push

알트 누르고 마우스로 범위지정하면 선택하여 한번에 바꾸기 및 제거
컨트롤 + 알트 누른 상태에서 화살표로 선택하여 한번에 바꾸기 및 제거
컨트롤 + F 에서 찾기, 바꾸기를 통해 원하는 코드 한번에 바꾸기
범위 지정 후 컨트롤 + ? : 범위 만큼 주석 처리하기
