'use strict';

import { question } from 'readline-sync';

// 문제30) 파이(π) 값을 소수점 다섯째 자리까지 출력하라.
console.log(`원주율은 ${(Math.PI * 1).toFixed(5)}`);

// Math.PI : Math.PI 속성은 원의 둘레와 지름의 비율, 약 3.14159의 값을 가짐
