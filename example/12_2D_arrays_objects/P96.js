'use strict';

// 문제96) 간단한 2차원 배열로 다음의 데이터를 생성하라.

const array = [
  [2, 5, 8],
  [3, 7, 4],
  [1, 6, 9],
  [4, 2, 0],
];
array.map((v) => console.log(v));
console.log(array);
// array 라는 배열 안에 3개의 배열이 각각 0, 1, 2, 3번째 위치에 포함되어 있는 구조
